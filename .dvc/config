[core]
    remote = storage

[remote "minio"]
    url = s3://mlops-data
    endpointurl = http://minio:9000
    access_key_id = admin
    secret_access_key = password

[remote "storage"]
    url = s3://mlops-data
    endpointurl = http://minio:9000
    access_key_id = admin
    secret_access_key = password
